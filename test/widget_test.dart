import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — Firebase requires initialisation before pumping EasyRideApp.
    // Add integration tests here once Firebase Test Lab is configured.
    expect(true, isTrue);
  });
}
